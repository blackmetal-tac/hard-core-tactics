using UnityEngine;

public class CrosshairAMS : MonoBehaviour
{
    public bool Taken;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
